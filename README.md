A simple Ruby class used to generate a Roman Numeral string from a given integer.

**Usage**:
```RomanNumeralGenerator.generate(123) # => 'CXXIII'```

**Test**:
Tested using RSpec, simply run `bundle exec rspec` in the current directory.
